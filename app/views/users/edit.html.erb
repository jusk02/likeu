
<section>
	<div class="container">
        <div class="row">
        	<div class="text-center">
	        	<p>
	        	Al fin te vamos a escuchar, por eso te pedimos que nos regales tu mejor voz para la
				siguente prueba, será algo muy simple pero debes estar muy atento.
				</p>
				<p>
				A continuación, cuando des click en el botón, aparecerá un texto corto que
				necesitamos que leas en voz alta para que te escuchemos fuerte y claro.
				</p>
				<p>
				Léelo a tu ritmo, no tienes que apurarte y si te equivocas fresco, solo sigue leyendo y
				ya está (la grabación se realiza una sola vez).
				</p>

				<% if current_user.video_done == false %>
					<%= form_for(current_user) do |f| %>
						<div class="field">
				    	    <br>
					        <%= f.hidden_field :video_done, :value => true %>
				    	</div>
			      		<br>
			      		<div class="actions">
			        		<%= f.submit 'Iniciar', :class => 'btn btn-primary' %>
			      		</div>
					<% end %>
				<% elsif current_user.video_done == true %>

					<%= form_for(current_user) do |f| %>
				  		
				      	<div class="field">
				        	<br>
				        	<%= f.hidden_field :video, :value => original_url %>
			    		</div>
					    <br>
					    <div class="actions">
					        <%= f.submit 'guardar', :class => 'guarda-video btn btn-success' %>
					    </div>
					     
					<% end %>
				<% end %>
				
				
				<a href="http://makemarkhosting.com/video/video3ddd/index.html?=<%= current_user.id %>" class="video-iniciar btn btn-primary btn-xl" target="_blank">Empezar</a>
				<%= original_url %>
        	</div>
    	</div>
	</div>
</section>

